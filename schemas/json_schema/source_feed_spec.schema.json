{
  "$defs": {
    "AuthSpec": {
      "additionalProperties": false,
      "description": "Reference to a Secret holding the auth token.",
      "properties": {
        "headerName": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Headername"
        },
        "secretKey": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Secretkey"
        },
        "secretName": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Secretname"
        },
        "type": {
          "default": "none",
          "enum": [
            "none",
            "bearer",
            "header",
            "basic"
          ],
          "title": "Type",
          "type": "string"
        }
      },
      "title": "AuthSpec",
      "type": "object"
    },
    "RateLimitSpec": {
      "additionalProperties": false,
      "description": "Politeness limits for a single SourceFeed.",
      "properties": {
        "burst": {
          "exclusiveMinimum": 0,
          "title": "Burst",
          "type": "integer"
        },
        "requestsPerSecond": {
          "exclusiveMinimum": 0.0,
          "title": "Requestspersecond",
          "type": "number"
        },
        "respectXPollInterval": {
          "default": false,
          "description": "If true, the poller defers to the response's X-Poll-Interval header.",
          "title": "Respectxpollinterval",
          "type": "boolean"
        }
      },
      "required": [
        "requestsPerSecond",
        "burst"
      ],
      "title": "RateLimitSpec",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "Spec of a single SourceFeed CRD instance.",
  "properties": {
    "acceptContentTypes": {
      "items": {
        "type": "string"
      },
      "title": "Acceptcontenttypes",
      "type": "array"
    },
    "auth": {
      "$ref": "#/$defs/AuthSpec"
    },
    "egressAllow": {
      "description": "Expected destination hostnames for policy generation and audit. Enforcement requires an FQDN-aware CNI; plain Kubernetes NetworkPolicy cannot match DNS names.",
      "items": {
        "type": "string"
      },
      "title": "Egressallow",
      "type": "array"
    },
    "enabled": {
      "default": true,
      "title": "Enabled",
      "type": "boolean"
    },
    "endpoint": {
      "format": "uri",
      "maxLength": 2083,
      "minLength": 1,
      "title": "Endpoint",
      "type": "string"
    },
    "licenseDefault": {
      "const": "per-record",
      "default": "per-record",
      "title": "Licensedefault",
      "type": "string"
    },
    "name": {
      "maxLength": 63,
      "minLength": 1,
      "title": "Name",
      "type": "string"
    },
    "pollIntervalSeconds": {
      "exclusiveMinimum": 0,
      "maximum": 86400,
      "title": "Pollintervalseconds",
      "type": "integer"
    },
    "protocol": {
      "enum": [
        "rss",
        "atom",
        "oai-pmh",
        "rest-json",
        "manual"
      ],
      "title": "Protocol",
      "type": "string"
    },
    "rateLimit": {
      "$ref": "#/$defs/RateLimitSpec"
    }
  },
  "required": [
    "name",
    "protocol",
    "endpoint",
    "pollIntervalSeconds",
    "rateLimit"
  ],
  "title": "SourceFeedSpec",
  "type": "object"
}
