---
# Keep the measured 10 GiB request for each member. The resulting aggregate
# capacity and the standalone-to-distributed copy duration need measurement
# before deployment to the retained cluster.
replicas: 4

persistence:
  storageClass: local-path
  size: 10Gi
