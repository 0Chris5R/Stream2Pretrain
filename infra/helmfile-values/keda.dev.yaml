---
operator:
  replicaCount: 1
metricsServer:
  replicaCount: 1
webhooks:
  enabled: true
  replicaCount: 1
prometheus:
  metricServer:
    enabled: true
    serviceMonitor:
      enabled: true
      namespace: monitoring
  operator:
    enabled: true
    serviceMonitor:
      enabled: true
      namespace: monitoring
  webhooks:
    enabled: true
    serviceMonitor:
      enabled: true
      namespace: monitoring
resources:
  operator:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 256Mi
  metricServer:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 256Mi
