apiVersion: v1
kind: Service
metadata:
  name: polaris-postgres
  labels:
    app.kubernetes.io/name: polaris-postgres
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: polaris-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres

